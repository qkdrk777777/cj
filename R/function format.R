#function(,,...){
  #...은 인수의 개수가 불특정할때 사용
  #return() 계산결과를 반환하고 종료
  #invisible() 계산결과를 반환
  #stop('') 함수를 종료하고 문자 출력
  #geterrmessage() 에러 메세지 반환
  #try() 예외처리. 에러발생시 메세지 출력
  #tryCatch()
  #topifnot() F일때 에러메세지 반환 및 종료
  #Sys.sleep() 지연함수
#}

